# Tests

Run the no-dependency physics tests with:

```powershell
node tests\physics.test.js
```

The tests load the browser source files into a Node VM context and exercise the core physics classes directly.
